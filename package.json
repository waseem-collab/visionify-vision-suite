{
  "name": "vision-suite",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Annotation Studio, Inference Web App (PPE + SM) and Crop Tools, served as one app on one port.",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "start": "python3 run.py",
    "setup": "pip install -r requirements.txt",
    "convex:dev": "convex dev",
    "convex:deploy": "convex deploy"
  },
  "keywords": ["yolo", "annotation", "cvat", "inference", "ppe", "computer-vision"],
  "license": "MIT",
  "devDependencies": {
    "convex": "^1.17.0"
  }
}
